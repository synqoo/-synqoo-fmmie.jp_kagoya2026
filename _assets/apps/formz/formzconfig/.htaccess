# 層 A 設定ファイル — 直リンク拒否（receive API 経由のみ更新）
<IfModule mod_authz_core.c>
    <FilesMatch "\.(txt|json|csv)$">
        Require all denied
    </FilesMatch>
    <Files ".kinkyuformz">
        Require all denied
    </Files>
</IfModule>
