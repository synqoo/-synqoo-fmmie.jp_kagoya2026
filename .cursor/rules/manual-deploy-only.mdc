---
description: 本番へのアップロードは手動のみ。エージェントはデプロイしない。
alwaysApply: true
---

# 本番デプロイ

- ローカルファイルを本番サーバーへ **アップロード・同期・デプロイしてはいけない**（SFTP、rsync、CI からの本番 push など含む）。
- 本番への反映は **ユーザーが手動**で行う。
- コード変更や Git の push は問題ないが、**サーバーへの転送操作は行わない**。
